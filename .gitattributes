# Linguist ignores Markdown as prose by default, which left the HTML sample as the only
# measurable thing and the language bar reading 100% HTML. This repo is Markdown, so say so.
*.md linguist-detectable=true

# Output: a rendered sample, not source.
skills/explain/example.html  linguist-documentation
